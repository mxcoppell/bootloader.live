# Research Report

This post does not require external research. The content is illustrative: it explains the blog's own format conventions. Sources for the format conventions are internal:

- `docs/superpowers/specs/2026-05-16-html-blog-format-design.md` — the canonical spec defining the format
- `.claude/skills/blog-illustration-style/SKILL.md` — Track A/B definitions
- `bootloader.live/index.html` — the SPA that renders the format

The illustrations and SVG diagrams demonstrate the format mechanically; their content does not need to be factually verified beyond "this is what a `hover-reveal` looks like."
